<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
</head>
<body>
<div>book list</div>
<ul>
    {% for book in books %}
        <li>{{ book.id }} - {{book.title}}</li>
    {% endfor %}
</ul>

</body>
</html>